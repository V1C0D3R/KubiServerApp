source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '8.0'

# enable Swift support and new dynamic frameworks
use_frameworks!

# ignore all warnings from all pods
inhibit_all_warnings!

target 'KubiServer' do
 pod 'GCDWebServer', '~> 3.0'
 pod 'lf', '~> 0.5.0'
end
